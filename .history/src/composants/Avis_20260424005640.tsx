import React from "react";

interface Props {
  icon: string;
  comme: string;
  image: string;
  name: string;
  profession: string;
}

export default function Avis({icon, text, image, name, profession}: Props) {
  return <div>Avis</div>;
}
